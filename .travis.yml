language: python
python:
    - '3.6'
install:
    - pip install -r api/requirements.txt
script:
    - python api/manage.py migrate --settings=api.settings.test
    - python api/manage.py test --settings=api.settings.test
    - flake8 api
    - ./frontend/node_modules/.bin/ng lint --type-check
